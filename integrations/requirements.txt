# Runtime candidates for compatibility testing, not a verified deployment lock.
# Pin all resolved transitive versions after the target OS and devices are tested.
livekit-agents==1.8.1
python-escpos==3.1
